# Local .env for running gql_evolution locally

# Demo mode enabled for local testing
DEMO=True

# Set default demo user to full admin (Ludvik Kilik) for easier testing
DEMO_DEFAULT_USER_ID=a0506cc4-5d53-4fdb-a989-c06a97e527fd

# Other defaults (adjust if needed)
POSTGRES_HOST=localhost:5434
POSTGRES_USER=postgres
POSTGRES_PASSWORD=example
POSTGRES_DB=data

# Endpoint for UG (if running separately)
GQLUG_ENDPOINT_URL=http://localhost:33001/api/gql/
# Development environment configuration
DEMO=True
DEMODATA=True

## Removed duplicate and conflicting database and endpoint settings below. Only correct values above are kept.
